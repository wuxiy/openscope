# 2026-08-29 — V0.1 Acceptance Remediation

## Route And Authorization

- Task type: bug investigation + deployment/integration implementation + verification/audit.
- Security/redaction is plan-first. cywu reviewed the ordered remediation from the completion audit and directly instructed “按顺序修复”.
- Active plan: `docs/plans/2026-08-29-1502-v0.1-acceptance-remediation-plan.md`.

## Implemented

- Redaction verification now separates Java Agent non-capture from Collector-side deletion, sends trace/metric/log OTLP controls and inspects full stored payloads.
- The sample sensitive endpoint no longer logs raw canaries and accepts Authorization/Cookie/token/password/body probes.
- Prometheus explicitly promotes the seven canonical Resource attributes; verification executes the three filtered Dashboard RED queries.
- Grafana verification uses datasource health endpoints and the provisioned Dashboard API.
- `doctor` now checks backend readiness, OTLP HTTP/gRPC, datasource health, Dashboard, `.env` permissions, runtime/data-volume writability, running image/BOM consistency and disk.
- The query helper is digest-pinned in the BOM.
- The remote runner now checks the entire tracked payload manifest, Agent checksum, clean current-source build, non-OpenScope snapshot equality and initial OpenScope state restoration.

## Verification State

- Local static/build commands passed; see `docs/testing/2026/08-29.md`.
- The disk guard was corrected to evaluate `/root/workspace/openscope-v0.1` rather than `/`; a read-only check shows its `/dev/vdb` filesystem has about 194 GiB available, so capacity is no longer a blocker.
- Scoped remote execution was explicitly authorized. Payload integrity, Agent checksum, BOM, Compose and docs gates passed, but the clean Maven build failed before container operations because only `java-21-openjdk-headless` is installed and the Java 21 development compiler is missing.
- Diagnosis established that a full Java 21 compiler must pass `javac --release 21`; the initial environment remained blocked pending an owner decision on system installation versus an isolated pinned JDK.
- cywu selected the recommended isolated-JDK path. Eclipse Temurin `21.0.12+8` Linux x64 and its official SHA-256 are now pinned in the BOM; the runner downloads, verifies and extracts it only under the owned remote dependencies directory and exports it for Maven and the sample runtime.
- The validation host could not transfer the official GitHub asset (zero bytes after about three minutes). A reachable Eclipse mirror was recorded as transport-only fallback while the official release checksum remains the acceptance authority.
- The isolated JDK and clean remote Maven build then passed. Runtime exposed a second disk-target drift in `cli/openscope`, which still checked `/`; start/doctor now check the filesystem containing the standalone workspace. No Compose mutation occurred and non-OpenScope snapshots remained identical.
- The next runtime reached 35/36 with doctor and all backend/Collector proofs green, then exposed two verifier false positives: overlapping app/Collector canaries and human-readable uptime in isolation snapshots. The canaries are now independent and snapshots use stable inspect fields.
- A subsequent run exposed persisted Grafana credential drift: a new `.env` password does not replace the password stored in an existing Grafana volume. The runner now synchronizes it with Grafana's official CLI, and Doctor validates authenticated API access without repeated 401 lockout attempts.
- After authorized password reset, Grafana and Doctor passed; E1 still found the independent application canary in the combined stored payload. The verifier now identifies trace/log/metric matches without revealing the canary.
- The first remote attempt exposed a preflight exit-code bug and stopped the OpenScope stack; its Grafana volume was deleted by the previous reset logic. Non-OpenScope resources were unchanged. The defect is fixed and the second attempt proved preflight now exits before mutation.
- Sanitized E1 diagnosis found the remaining Loki record came from a 2026-08-28 orphaned `openscope-sample` process on port 8090, not the newly built JAR. Verification now refuses an occupied sample port and requires its exact spawned PID to remain alive through readiness; the confirmed OpenScope orphan was removed.
- Final machine evidence is `docs/testing/2026/08-29/remote-20260829-192709.txt`: payload manifest `91d1742400d928dedc9c27e32e1056750cedb874831ebaf85656c28f2da4c413`, clean isolated-JDK build, healthy Doctor, two consecutive `36/36` runtime passes, and fail-closed/recovery proof for Collector, Prometheus, Tempo, Loki and Grafana.
- Grafana's persisted admin credential was synchronized to the run-local `.env` value before authenticated API checks. The password remained remote-only and was not written to evidence.
- Cleanup preserved OpenScope volumes, restored its initial stopped state and proved the non-OpenScope container snapshot unchanged.
- cywu completed the independent human closure review and confirmed “已验证，请提交并推送”. V0.1 AC-01..AC-16 is accepted; both plans, the checklist, owner status and backlog were closed before commit.
