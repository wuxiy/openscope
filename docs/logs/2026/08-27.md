# 2026-08-27 — AGE 初始化 + Onboarding

## 今日工作

1. **架构文档评审与修订**（AGE 之前）：
   - 发现并修复：Agent/Starter 叠加隐患、Tail Sampling 与 Distributed V1.0 时间线矛盾、SLO 无落地、strict 场景脱敏时机太晚、仓库结构双文档漂移
   - cywu 决策落档：Agent+Starter 双模式并存（互斥）；版本锁最新稳定（Prometheus >3.x 原生 OTLP Receiver、Loki ≥3.0 原生 OTLP 端点）；Metrics 管线切 `otlphttp/prometheus`
2. **AGE 脚手架安装**：`age-init . OpenScope`，88 files，零跳过
3. **引擎冒烟**：`mission-driver.sh run demo` → reconciled: failed→completed（CHECK 的外部 opencode CLI 报 server error，已知环境问题，不影响文件产物）
4. **Onboarding WI1–WI8 全部完成**：
   - WI1 项目扫描 → docs/input/project-scan.md
   - WI2 project-context.md（identity/baseline/验证命令/项目特定红线）
   - WI3 ai-autonomy-policy.md（reviewer=subagent；保护区：版本策略 ask-first、安全脱敏 plan-first 等）
   - WI4 codebase-map.md（pre-code 版本：入口=两份根架构文档）
   - WI5 docs/index.md（移除 Domain Quick-Reference 段）
   - WI6 project-vision / system-baseline / module-boundaries / architecture README
   - WI7 backlog README 首批切片（P0=V0.1 standalone 发行版 needs-requirement）
   - WI8 known-good-baselines.md + logs 目录确认
   - conventions.md 追加 Language Rule（自产文档中文）

## 验证状态

- `./tools/mission-driver.sh list/run demo` 绿；build/test 类命令 V0.1 出码前为 none
- 已知失败：opencode CLI server error（外部环境，已记录 baseline）

## 下一步

- 从 backlog P0 开始：为「V0.1 最小 standalone 发行版」做需求综合（docs/requirements）
- 出码后补齐 project-context 验证命令表并重写 codebase-map 真实路径

## 经验记录

- onboarding 模板的权威检查是"占位符移除"，不是"内容长度"；逐个表格核对比整体重写更不易漏

## AGENTS.md 渐进式披露重构（17:50）

- 矛盾裁决（cywu）：① 定位句改为发行版/框架 ② active work 以 plans/backlog 为准，project-context 不加字段 ③ 分组文件进 docs/agents/
- 根文件 177 行 → 47 行：只留定位一句话、必读三件套、环境事实（Maven 非 npm、mission-driver 命令）、5 条行为硬规则
- 新增 docs/agents/{task-routing,workflow,planning-and-audit,docs-structure-and-maintenance}.md
- 删除项见当日会话记录：comment 规则（conventions.md 已覆盖）、日志规则重复两处合一、"reverse chronological" 模糊词、prompting guidance 通识四条、4–5 文件灰区（trigger 收敛为 >3 files）

## V0.1 实施准入合同、计划与验收（19:37）

- 建立实施级 requirement、standalone 技术 owner contract、draft plan、稳定验收 checklist 和文档自检脚本。
- V0.1 收敛为 standalone + Java Agent + OTLP 三信号 + P/T/L/Grafana + 基础 Logs ↔ Trace + 最小 CLI；Central、Starter、filelog、离线/备份/升级移出本里程碑。
- 冻结 2026-08-27 候选 tag：Collector `0.159.0`、Prometheus `v3.14.0`、Loki `3.7.6`、Tempo `3.0.3`、Grafana `13.2.0`、Java Agent `2.31.1`、Spring Boot `4.1.1`、Java `21`。
- Docker Registry 本轮不可达；未伪造 manifest digest。Phase 0 必须物化 exact tag + multi-arch digest、双架构与来源证据，否则不得首次启动 Compose。
- 根架构和 AGE owner docs 已对齐：Prometheus 下限统一为 `≥ 3.0`；V0.1 日志唯一走 Java Agent OTLP；mission-driver demo 明确仅是 AGE scaffold smoke。
- Audit verdict：文档可用于计划评审；实施仍不可开始。plan 保持 `draft`，等待 cywu 独立 human draft review；运行验收全部未执行。
- 本轮已执行：`sh -n tools/verify-docs.sh`、`./tools/verify-docs.sh`、`git diff --check`、`./tools/mission-driver.sh list`，均退出 0；未执行 `mission-driver run demo`，未执行任何产品 build/runtime/E2E（相关代码尚不存在）。
