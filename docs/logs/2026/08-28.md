# 2026-08-28 — V0.1 开发环境准入

## 开发机只读探测（00:34）

- 用户指定完整实施验证环境：`root@172.16.65.59` 免密 SSH。
- 实测为 CentOS Stream 9 x86_64、16 vCPU、62 GiB RAM、Java 21.0.11、Docker 29.5.3、Compose 5.1.4、Buildx 0.34.1；Maven Central 可达。
- 该机是共享开发环境，当前有 33 个运行容器；`3000` 被 `multica-frontend-1` 占用，`13000` 也被占用，`13001/4317/4318` 探测时空闲。
- 根盘剩余约 14 GiB、使用率 83%、无 swap；未执行 prune、pull、目录创建、容器操作或任何远端写入。
- Docker Registry 直连超时，Buildx 对 Prometheus 候选 tag 的 manifest inspect 超时退出 124；Phase 0 镜像解析仍 blocked。
- requirement、owner contract、draft plan、acceptance checklist、backlog 与 project context 已改为：远端 x86_64 必须完成真实验收，使用 `/opt/openscope-v0.1` + `openscope-v01` + Grafana `13001`，并保护所有非 OpenScope 资源。

## 远端依赖目录调整

- cywu 指定依赖文件放在开发机 `/root` 下；active contract 改用 `/root/openscope-v0.1`，依赖集中存放 `/root/openscope-v0.1/dependencies`，不散落到 `/root` 根层。
- Compose project 仍为 `openscope-v01`；只读确认 `/root/openscope-v0.1` 当前不存在。本轮只更新文档，没有在远端创建目录或传输文件。

## 远端工作目录最终调整

- cywu 将工作目录改为 `/root/workspace/openscope-v0.1`；依赖目录同步为 `/root/workspace/openscope-v0.1/dependencies`。
- 只读确认新目标路径当前不存在；active contract 已更新，仍未在远端创建目录或传输文件。
