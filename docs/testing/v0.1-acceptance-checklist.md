# V0.1 Standalone Acceptance Checklist

> Checklist Status: executed 2026-08-28 — ALL PASSED (amd64 real-run; arm64 manifest-evidence only)
> Last Reviewed: 2026-08-28
> Requirement: `docs/requirements/2026-08-27-1937-v0.1-standalone-distribution.md`
> Plan: `docs/plans/2026-08-27-1937-v0.1-standalone-distribution-plan.md`

## Evidence Rules

- 每项必须记录命令、退出码、关键响应摘要和执行环境。
- 实际执行、跳过、失败、人工未执行分开记录。
- 容器 healthy 只证明进程健康，不证明三信号已落后端。
- 任一安全、版本一致性、信号丢失或关联失败不得降级为警告。
- 完整验收记录写入 `docs/testing/YYYY/MM-DD.md`，本文只保存稳定清单。

## Requirement Coverage

| Requirement AC | Checklist Evidence |
| --- | --- |
| AC-01 BOM | A1–A5 |
| AC-02 Config | B3–B5 |
| AC-03 Start/Stop | C1–C3, G4 |
| AC-04 Network | C4–C6 |
| AC-05 Trace | D2–D3 |
| AC-06 Metrics | D4–D5 |
| AC-07 Logs | D6–D8 |
| AC-08 Correlation | D7, F5–F6 |
| AC-09 Redaction | E1–E4 |
| AC-10 Grafana | F1–F4 |
| AC-11 Doctor | G2–G3 |
| AC-12 Persistence | H1–H3 |
| AC-13 Re-run | H4–H5 |
| AC-14 Architecture | A3, H6 |
| AC-15 Evidence | H7–H8 |
| AC-16 Remote Isolation | I1–I8 |

## A. Release Reproducibility

- [x] A1 `distribution/bom.yaml` 包含所有组件 exact tag、manifest digest、来源 URL、解析日期。
- [x] A2 Compose 中每个 image 使用 `tag@sha256:digest`，无 `latest`、`${managed}` 或浮动 major/minor tag。
- [x] A3 `amd64` 和 `arm64` 均存在于所锁定 manifest。
- [x] A4 OTel Java Agent JAR SHA-256 等于 `bbf83c151b6400709e2f225bdd07a04f839d9d13b8b93464241333fd25d3e3ba`。
- [x] A5 Collector 使用的每个 receiver/processor/exporter 均记录信号级 stability，无未裁决的 alpha 依赖。

## B. Static And Build Verification

- [x] B1 `./tools/verify-docs.sh` 退出 0。
- [x] B2 `./mvnw -q -pl examples/springboot-simple -am verify` 退出 0，无 skipped test 被当作已执行。
- [x] B3 `docker compose --env-file distribution/standalone/.env.example -f distribution/standalone/docker-compose.yml config --quiet` 退出 0。
- [x] B4 Collector、Prometheus、Tempo、Loki 配置通过各自官方镜像/命令的静态校验。
- [x] B5 shell 脚本通过项目选定的语法/静态检查，命令已写入 `project-context.md`。

## C. Startup And Network Boundary

- [x] C1 空数据目录下 `./cli/openscope start` 退出 0。
- [x] C2 再次执行 `start` 幂等，不创建重复容器/volume/network。
- [x] C3 `status` 显示五个组件的实际状态。
- [x] C4 默认 profile 只监听 `127.0.0.1:3000/4317/4318`；开发机 profile 只监听 `127.0.0.1:13001/4317/4318`，`9090/3100/3200` 等后端端口未 publish。
- [x] C5 Grafana 空密码或默认密码时 `start` 拒绝启动。
- [x] C6 端口冲突、volume 不可写、Docker 不可用的失败路径均非零退出并给出可操作原因。

## D. Telemetry Data Proof

- [x] D1 示例应用由 Java Agent 2.31.1 启动，未引入 OTel/OpenScope Starter SDK。
- [x] D2 成功 HTTP 请求在 Tempo 中有 SERVER span，包含七个必需 Resource 属性。
- [x] D3 失败 HTTP 请求在 Tempo 中体现错误状态/异常证据。
- [x] D4 Prometheus 查到 `target_info` 与 HTTP server Rate/Error/Duration 指标。
- [x] D5 Prometheus 七个 Resource attributes 按预期提升/规范化，六元组可过滤。
- [x] D6 Loki 查到通过 Java Agent OTLP log exporter 发送的日志，未使用 filelog/stdout 采集路径。
- [x] D7 Loki log record 包含与 Tempo 相同的 `trace_id`、对应 `span_id` 和 Resource 属性。
- [x] D8 Prometheus/Loki/Tempo 查询采用后端 API 实际返回数据，不仅是 health endpoint。

## E. Security And Redaction

- [x] E1 Java Agent 默认配置未捕获 Authorization、Cookie、Token、request/response body、SQL bind parameters。
- [x] E2 Collector 第二道防线删除已知敏感 key，并对 traces/metrics/logs 分别有测试输入。
- [x] E3 带唯一合成敏感 canary 值的请求后，Prometheus/Loki/Tempo 均无明文匹配。
- [x] E4 Grafana 密码、镜像 registry token 和其他敏感值未进入 Git diff、日志或验收证据。

## F. Grafana And Correlation

- [x] F1 Prometheus/Tempo/Loki datasource 均由 Git 中 YAML Provisioning，UID 与 owner contract 一致。
- [x] F2 三个 datasource 在 Grafana API 中返回成功健康状态。
- [x] F3 Overview/Service Dashboard 被 Provisioning，不是 UI 手工创建。
- [x] F4 site/environment/project/namespace/service/instance 六变量均有值且对应必需 Resource 属性。
- [x] F5 Loki 日志行的 `trace_id` 可内部跳转 Tempo。
- [x] F6 Tempo span 可按时间窗口、`service_name` 和 trace id 跳转 Loki。

## G. CLI And Failure Semantics

- [x] G1 `version` 输出 OpenScope 版本与五个 BOM 组件 tag/digest 摘要。
- [x] G2 正常栈 `doctor` 退出 0，检查 Collector/Prometheus/Tempo/Loki/Grafana/OTLP/datasource/dashboard/disk/permissions。
- [x] G3 停止任一后端后 `doctor` 退出非 0，且只报告真实失败/依赖失败。
- [x] G4 `stop` 可重复执行，默认不删除持久化数据。
- [x] G5 命令失败不被 reconciliation 或 warning-only 转换为完成。

## H. Persistence, Repeatability And Evidence

- [x] H1 Compose restart 后三 datasource 和 Dashboard 仍存在。
- [x] H2 restart 后在 retention 内的历史 metrics/logs/traces 仍可查询。
- [x] H3 restart 后生成的新三信号均可再次查询。
- [x] H4 `./tools/verify-v0.1.sh` 连续运行两次通过。
- [x] H5 失败注入时脚本仍执行清理，保留足以诊断但不含敏感值的证据。
- [x] H6 在 `root@172.16.65.59` 完成 amd64/x86_64 真实运行验收；arm64 至少完成 manifest 证据，若未在本地 Mac 真实运行则明确标记未执行。
- [x] H7 `docs/testing/YYYY/MM-DD.md`、实施 plan、dev log 和 owner docs 中的验证状态一致。
- [x] H8 独立 closure audit 核对每个 AC 的原始证据后才允许关闭 plan。

## I. Remote Development Environment Isolation

- [x] I1 SSH 使用 `BatchMode=yes`、`StrictHostKeyChecking=yes`、`ForwardAgent=no` 成功；没有写入或打印私钥、密码和 token。
- [x] I2 远端证据记录 OS、arch、CPU、memory、Java、Docker/Compose/Buildx 版本和执行时间。
- [x] I3 start 前确认 `13001/4317/4318` 可用，根盘至少 10 GiB 可用且使用率低于 90%；否则 fail closed。
- [x] I4 所有远端文件只位于 `/root/workspace/openscope-v0.1`，Java Agent、镜像归档、校验文件及其他依赖只位于 `/root/workspace/openscope-v0.1/dependencies`；所有 Compose 资源只属于 project `openscope-v01`。
- [x] I5 远端 payload 的 Git revision/内容摘要与本地待验版本一致，不使用开发机上来源不明的 checkout。
- [x] I6 完整 A–H 验收由远端 runner 执行，命令、退出码和脱敏摘要回收到 `docs/testing/`。
- [x] I7 执行前后的非 OpenScope container ID/status 对比一致；未 stop/recreate/delete 任何非 OpenScope 资源。
- [x] I8 验收未运行 `docker system prune`，未删除共享镜像/volume/network；停止测试栈时只作用于 `openscope-v01` 且默认保留其数据。

## Final Verdict

- [x] `accepted`：A-I 全部关闭，无确认缺陷或被模糊化的 follow-up。
- [ ] `conditionally accepted`：仅存已裁决 residual risk，且不影响 V0.1 所承诺的结果面。
- [ ] `rejected`：任一非可降级项失败。
