# 2026-08-29 — V0.1 Acceptance Remediation Verification

> Plan: `docs/plans/2026-08-29-1502-v0.1-acceptance-remediation-plan.md`
> Scope: AC-06/09/10/11/15/16 remediation
> Current verdict: accepted 2026-08-29 after full machine verification and independent human closure audit

## Local Verification

Executed against the current working tree on macOS:

| Command | Result |
|---|---|
| `bash -n cli/openscope tools/verify-v0.1.sh tools/verify-v0.1-remote.sh tools/resolve-bom.sh` | passed |
| `./tools/resolve-bom.sh --check` | passed, including digest-pinned `query-curl` helper |
| `./tools/verify-docs.sh` | passed |
| `./mvnw -q -pl examples/springboot-simple -am verify` | passed |
| `docker compose --env-file distribution/standalone/.env.example -f distribution/standalone/docker-compose.yml config --quiet` | passed |
| `git diff --check` | passed |

These are scoped/static results. They do not prove the changed Collector, Prometheus, Grafana or doctor runtime behavior.

## Remote Attempt 1 — Runner Fail-Closed Defect Found

- Evidence: `docs/testing/2026/08-29/remote-20260829-151105.txt`
- Preflight correctly detected `available=9GiB`, below the 10 GiB contract gate, but the runner wrapped the failing SSH command in `say "$(...)"`; the logging function returned success and execution continued.
- Before the later Maven failure, the old runner logic synchronized the payload, stopped the OpenScope stack and deleted only `openscope-v01_grafana-data`.
- Cleanup recorded the complete non-OpenScope container snapshot unchanged.
- Post-attempt read-only check found no OpenScope containers running; `openscope-v01_prometheus-data`, `openscope-v01_tempo-data` and `openscope-v01_loki-data` remain. The Grafana volume was removed and is not recoverable from Docker after deletion.
- No prune command or non-OpenScope container/volume/network operation ran.

Disposition: confirmed runner defect, fixed in the working tree. Preflight now captures the SSH exit status outside the logging function and exits before payload sync or container operations. Static/build gates also run before runtime mutation, Grafana volume deletion was removed, and the runner restores the initial OpenScope running/stopped state.

## Remote Attempt 2 — Correct Fail-Closed Result

- Evidence: `docs/testing/2026/08-29/remote-20260829-151313.txt`
- Host: Linux x86_64, Java 21.0.11, Docker 29.5.3, Compose 5.1.4, Buildx 0.34.1.
- Disk: 9.9 GiB displayed, 9 GiB by the integer guard, usage 88%.
- Ports `13001/4317/4318`: free because the OpenScope stack was stopped by attempt 1.
- Result: exit 1 with `REMOTE ACCEPTANCE BLOCKED before payload sync or container operations`.
- No payload sync, remote file write or container operation followed the failed preflight.

## Workdir Filesystem Correction

- The first two attempts evaluated `df /`, but the declared remote workspace is `/root/workspace/openscope-v0.1` and may reside on another filesystem.
- The runner now requires that directory to exist and evaluates both available capacity and utilization on the filesystem containing it.
- A read-only check on 2026-08-29 returned `/dev/vdb`, 200 GiB total, about 194 GiB available and 3% utilization. The 10 GiB / 90% capacity gate is therefore satisfied.
- The earlier `/` result remains evidence for the exit-code regression, but it is not a valid capacity blocker for the declared workdir.
- Full runtime acceptance can proceed only with explicit authorization because it starts, force-recreates and may stop the OpenScope Compose project. Local/static success must not be reported as V0.1 acceptance.

## Remote Attempt 3 — Java Development Kit Missing

- Evidence: `docs/testing/2026/08-29/remote-20260829-180731.txt`.
- Explicit authorization for scoped OpenScope container operations was received before this attempt.
- Workdir capacity passed on `/dev/vdb`: about 194 GiB available, 3% utilization.
- Payload manifest, Java Agent SHA-256, BOM, Compose configuration and docs verification passed against the current dirty working-tree payload.
- The clean Maven build failed before container operations with `release version 21 not supported`.
- Read-only diagnosis found only `java-21-openjdk-headless` installed. The runtime contains the `jdk.compiler` module but lacks `/usr/bin/javac` and `lib/ct.sym`; invoking the compiler module with `--release 21` reproduces the same failure.
- Root cause classification: remote environment prerequisite missing, not a source-code or test failure. No OpenScope or non-OpenScope container was started, recreated or stopped during this attempt.
- The selected remediation is a digest-pinned isolated JDK under the owned dependencies directory; the runner validates `javac --release 21` before the clean build without requiring a system package.

## Remote Attempt 4 — Official Download Path Unreachable

- Evidence: `docs/testing/2026/08-29/remote-20260829-181504.txt`.
- The digest-pinned isolated Eclipse Temurin JDK path was selected and the payload manifest passed.
- The official GitHub release download remained at zero bytes for about three minutes, so the attempt was interrupted before build or container operations.
- Read-only probes found the Nanjing University and Alibaba Cloud Eclipse mirrors serving the same 207,486,543-byte archive. The BOM now records the official source plus a transport mirror; the official SHA-256 remains mandatory and authoritative.

## Remote Attempt 5 — CLI Disk Target Drift

- Evidence: `docs/testing/2026/08-29/remote-20260829-182111.txt`.
- The mirror-delivered JDK matched the official SHA-256, extracted under the owned dependencies directory and reported Eclipse Temurin/Javac `21.0.12`.
- Remote BOM, Compose, docs and clean Maven gates passed with the isolated JDK.
- Runtime stopped at `cli/openscope start` before Compose mutation because the CLI still evaluated `df /` and reported 9 GiB, despite the repository residing on `/dev/vdb` with about 194 GiB available.
- Cleanup preserved the initial stopped OpenScope state. The complete non-OpenScope container snapshot was byte-for-byte unchanged.
- Fix: CLI start and doctor now evaluate the filesystem containing `distribution/standalone`, matching the remote runner and owner contract.

## Remote Attempt 6 — Verification False Positives

- Evidence: `docs/testing/2026/08-29/remote-20260829-182328.txt`.
- Isolated JDK, clean build, stack start, expanded doctor, Tempo, Prometheus six-dimensional RED queries, Loki correlation, Grafana health and all direct Collector redaction controls passed.
- The run reported 35/36 because the Collector canary was constructed by appending a suffix to the application canary. The E1 application search therefore matched the intentionally stored direct-OTLP value even though the two proof paths must be independent.
- Linux also lacked macOS `shasum`, so the displayed application-canary fingerprint was empty. Fingerprinting now uses the already-required Python runtime.
- The non-OpenScope snapshot included human-readable uptime. One unrelated container naturally crossed from `Up 47 hours` to `Up 2 days`, causing a false difference despite the same container ID and running state.
- Fix: application and Collector canaries are now unrelated values; isolation compares stable container ID, state, running flag, restart count and configured image. Cleanup restored the initially stopped OpenScope state and preserved volumes.

## Remote Attempt 7 — Persisted Grafana Credential Drift

- Evidence: `docs/testing/2026/08-29/remote-20260829-182713.txt`.
- Stable non-OpenScope snapshot comparison passed.
- Grafana `/api/health` was reachable, but authenticated datasource and Dashboard calls returned 401. Logs showed repeated incorrect-password attempts followed by Grafana's five-minute username lockout.
- Root cause: the runner generated a new `.env` password each run, while Grafana applies `admin_password` only on first database initialization. The preserved Grafana volume retained the earlier password.
- Fix: after recreation, the runner uses Grafana's official CLI to synchronize the persisted admin password to the current `.env` before Doctor. Doctor now tests the authenticated `/api/user` endpoint, fails immediately on 401 and skips dependent checks instead of causing repeated lockout attempts.

## Remote Attempt 8 — Application Canary Requires Signal Localization

- Evidence: `docs/testing/2026/08-29/remote-20260829-190416.txt`.
- Explicitly authorized Grafana admin reset succeeded. Expanded Doctor, authenticated Grafana health, all three datasource health checks and Dashboard provisioning passed.
- Backend, correlation, RED-query and direct Collector deletion checks passed, but E1 still found the independent application canary in the combined stored payload.
- Non-OpenScope snapshot equality passed and cleanup restored the initially stopped OpenScope state.
- The verifier now reports the matching signal type without printing the raw canary so the remaining capture path can be diagnosed safely.

## Remote Attempts 9–10 — False E1 Localized To A Stale Sample Process

- Evidence: `docs/testing/2026/08-29/remote-20260829-190749.txt` and `remote-20260829-191105.txt`.
- Sanitized inspection localized the application canary first to Loki and then to `log.body`; no match existed in traces, metrics or Loki stream labels.
- The matching log scope was `dev.openscope.sample.ProbeController`, and the stored body shape matched the pre-remediation log statement. Read-only host inspection found an orphaned OpenScope sample process, PID `786886`, started on 2026-08-28 from `/root/workspace/openscope-v0.1`, still listening on `8090` with the old JAR.
- Root cause: a newly launched sample could exit on port collision while the verifier's readiness request succeeded against that old process. The verifier therefore produced a false execution claim against a process it did not own.
- Fix: preflight now requires that the sample port be free, and readiness requires the exact spawned PID to remain alive. After re-validating the PID's workdir and OpenScope service identity, the orphan was terminated. Non-OpenScope state was not touched.

## Remote Attempt 11 — Two Consecutive Runtime Passes

- Evidence: `docs/testing/2026/08-29/remote-20260829-191828.txt`.
- Payload manifest, isolated JDK, Agent checksum, clean Maven build, Compose, docs and expanded Doctor all passed.
- Both runtime executions passed `36/36`, including application non-capture, Collector trace/metric/log deletion controls, six-dimensional RED queries, Loki/Tempo correlation and authenticated Grafana checks.
- The runner restored the initially stopped OpenScope state and the stable non-OpenScope snapshot was unchanged.
- During documentation reconciliation, the runner was found not to execute the plan's five-component Doctor failure injection, so this pass was retained as valid runtime evidence but not treated as the final A–I closure record.

## Remote Attempt 12 — Failure-Injection Harness Quoting Defect

- Evidence: `docs/testing/2026/08-29/remote-20260829-192308.txt`.
- Both product-integration executions passed `36/36`. Collector injection then made Doctor exit non-zero and report the stopped component plus unreachable OTLP listeners as expected.
- The harness incorrectly quoted a dynamic `grep` pattern, so its evidence assertion was split into multiple arguments and the runner exited 1 before recovery. Cleanup restored the initially stopped OpenScope state; non-OpenScope snapshot equality passed.
- Fix: use explicit per-component evidence-file variables and a fixed fail-closed marker match.

## Remote Attempt 13 — Final Machine Verification

- Evidence: `docs/testing/2026/08-29/remote-20260829-192709.txt`.
- Environment: Linux x86_64; Docker `29.5.3`; Compose `5.1.4`; Buildx `0.34.1`; isolated Eclipse Temurin/Javac `21.0.12+8`; workdir filesystem `/dev/vdb`, about 194 GiB available, 4% usage.
- Payload: Git HEAD label `7d52e669bdbbdfb37e09bd29b8a9affe3a62603f` with a dirty working tree; transferred-content manifest SHA-256 `91d1742400d928dedc9c27e32e1056750cedb874831ebaf85656c28f2da4c413`; every tracked file and all listed key-file digests matched remotely.
- Static/build gates: BOM, Compose config, docs, Maven Wrapper runtime and clean Maven verify all exited 0. Java Agent SHA-256 matched the committed manifest.
- Healthy Doctor: five containers, four backend APIs, both OTLP listeners, authenticated Grafana API, three datasource health endpoints, Dashboard provisioning, permissions, four data volumes, five BOM image identities and workspace disk guard all passed.
- Runtime: two consecutive executions each passed `36/36`. Application Authorization/Cookie/token/password/response-body canaries were absent from complete Tempo/Loki/Prometheus payloads; direct OTLP trace/metric/log controls arrived and their sensitive attributes were absent.
- Failure semantics: Collector, Prometheus, Tempo, Loki and Grafana were each stopped separately. Doctor returned non-zero and named the stopped component every time, then returned to full green after that component restarted.
- Cleanup/isolation: volumes were preserved, the initially stopped OpenScope state was restored, and the before/after non-OpenScope container snapshot was byte-for-byte unchanged. No prune or foreign-resource mutation ran.
- Post-run maintenance changed only review/status/evidence documentation. No executable, configuration, BOM, sample or Dashboard file changed after the remote payload was tested; `verify-docs`, shell syntax and `git diff --check` were rerun locally after documentation reconciliation.

## Verification Boundary

- Executed and passed: local syntax, BOM, docs, Maven, Compose and diff checks; remote clean build; healthy Doctor; two `36/36` runtime runs; five failure injections; payload/Agent/JDK integrity; cleanup and shared-host isolation.
- Skipped: no configured V0.1 machine check was skipped. Arm64 remains manifest-only as explicitly permitted by AC-14; no arm64 runtime was claimed.
- Failed but superseded: Attempts 1–10 remain regression evidence and are not counted as passing execution.
- Independent closure audit: cywu confirmed “已验证，请提交并推送” on 2026-08-29 after reviewing the completion and quality evidence. AC-01..AC-16 and A1..I8 are accepted; both plans and the checklist are closed.
