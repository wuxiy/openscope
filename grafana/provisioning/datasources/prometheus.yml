# Prometheus datasource — fixed UID per V0.1 contract.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: openscope-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 15s